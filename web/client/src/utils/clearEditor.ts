
// const clearEditor = (editor: BlockNoteEditor) => {
//   // 步骤 A：从 editor.document 中提取出所有块的 ID
//   const blockIds = editor.document.map((block) => block.id);
//   // 步骤 B：调用 editor.replaceBlocks(...)
//     editor.replaceBlocks(blockIds, []);
//     editor.focus();
// }